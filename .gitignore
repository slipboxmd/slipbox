node_modules/
dist/
.turbo/
*.log
.DS_Store
.env
.env.local

# Local pi session state / trust
.pi/sessions/
.pi/trust.json

# QMD's derived index (source of truth is the markdown; the index is a cache).
# The .qmd/index.yml may be committed; the sqlite database should not be.
.qmd/index.sqlite
.qmd/*.sqlite-*

# Never track a stray QMD index in the harness repo itself
.qmd/
